db:
  # Set ARES_DB_CONN_STR or provide a local, untracked configuration file.
  conn_str: ""

web:
  address: ":8080"

log:
  level: "info"
  accessLogfile: "stdout"
  runtimeLogfile: "stderr"

job:
  taskManager:
    cron: "@every 10s"

demo_data:
  enabled: false

settings:
  # Prefer the environment variables documented in .env.example.
  admin_token: ""
  encryption_key: ""
