# W11：CI、产出物与 CD 解耦实施计划

## 1. 状态与范围

2026-09-16：B1 [PR #60](https://github.com/go-ree/ares/pull/60) 与 B2 [PR #61](https://github.com/go-ree/ares/pull/61) 已合并，基线 `main@0e9eea2`。B 拆为 B1 绑定意图预检/参数规则、B2 绑定持久化、B3 独立运行；B1/B2 已交付，**B3 与 C 按维护者 2026-09-16 决定暂缓**，当前最高优先级改为[前端框架迁移](../architecture/decisions/0008-frontend-react-semi-migration.md)（ADR-0008，批次 B0～B5）。[Java 步骤清单](java-ci-step-catalog.md) 仍是后续契约计划，不表示真实执行可用。

W11 是在原 W00～W10 编号之外新增的产品模型工作包，编号不表示排期在 W10 之后。优先顺序调整为：**前端框架迁移（B0～B5，当前最高优先级）** → W11-B3/C/D/E（无外部依赖的完整模拟链路，其中 D 直接在新栈上开发）→ W07-C 与 W08 → W11-F（真实构建/加工/部署接入）→ W09/W10。前端迁移与后端 W11 链路不共享文件，B3/C 暂缓不等于放弃其承诺，恢复时按原验收门禁推进。W07-A/B 已完成并继续复用；W07-C 暂缓，不删除其承诺，也不宣称完整 W07 完成。

W07-C 在 W11 运行模型固定后同时覆盖 CI/CD 的取消；W08 是真实流程的通用凭据解析门禁。W11-F 借用 W09 的适配器契约测试方向，但不承诺在本工作包完成所有执行平台 SDK 或生产发行治理。W01 仓库管理项继续独立跟踪。

## 2. 分阶段交付看板

每阶段至少一个中文 PR；涉及多个独立迁移或前后端复杂度时可再拆分，必须先更新本表。时间以验收门禁推进，不给尚无依据的工期承诺。

| 阶段 | 依赖 | 交付范围 | 验收门禁 | 状态 / PR |
| --- | --- | --- | --- | --- |
| W11-0 | 用户确认方向 | 架构、决策、兼容及实施计划 | 文档评审合并；现有预览不变 | 已完成 / [PR #40](https://github.com/go-ree/ares/pull/40) |
| W11-A | W11-0 | 类型目录、CI/CD 模板及不可变版本、参数/产物槽位规范；冻结 DTO、权限矩阵与资源限制 | 类型不写死，CI/CD 模板归属清晰，版本/CAS/权限契约及迁移测试 | A1/A2/A3 已合并 |
| W11-B | A | 应用 CI/AppConfig CD 绑定，独立运行上下文与创建回执，服务端预检 | CI 无需环境，CD 必须声明输入；命令隔离、资源关系、并发幂等、旧流程兼容 | B1/B2 已合并；B3 暂缓（让位于前端迁移） |
| W11-C | B | 产物目录、候选发布、派生关系、类型化步骤交接；模拟 CI/加工/部署适配器 | 发布门禁、登记原子性、重启接管、摘要冲突、父链与最终部署引用 | 未开始 |
| W11-D | C、[ADR-0008](../architecture/decisions/0008-frontend-react-semi-migration.md) | 集中类型/模板管理、应用 CI、产物列表详情、环境 CD 配置与发布 UI | 权限、空状态、版本选择、输入兼容性和来源链的前端与 API 联调 | 未开始 |
| W11-E | D | 显式旧模式转换工具/指引、Demo、全栈验收与预览交付 | 存量兼容、一次 CI 多环境 CD、加工与回退路径、升级恢复矩阵 | 未开始 |
| W11-F | E、W07-C、W08 | 一条真实构建→受管产物→镜像加 Agent→部署的适配链 | 真实产物摘要验证、私密参数隔离、取消确认与外部副作用恢复验收 | 未开始 |

### 2.1 A：模型与契约

2026-09-11 开始实施，拆为 A1 模板契约与无副作用校验、A2 类型/模板持久化与版本 CAS、A3 管理接口及权限矩阵。A1 不写数据库、不登记产物、不启动任务；其校验成功仅表示结构合法，不能当作可执行或已发布模板。A1/A2/A3 已合并，完整 A 不代表构建/部署可用。

| 增量 | 状态 | 范围 |
| --- | --- | --- |
| A1 | 已合并：[PR #42](https://github.com/go-ree/ares/pull/42) | 独立 CI/CD 规范、类型化槽位、前序引用、参数约束、校验 API 与回归测试 |
| A2a | 已合并：[PR #44](https://github.com/go-ree/ares/pull/44) | 类型/模板/不可变版本 schema、数据契约、最小权限与恢复验收，详见 [存储契约](../development/template-storage.md) |
| A2b | 已合并：[PR #53](https://github.com/go-ree/ares/pull/53) | 事务存储、类型启停与模板草稿 CAS、原子发布版本及并发测试；无 HTTP/UI/执行路径 |
| A3 | 已合并：[PR #59](https://github.com/go-ree/ares/pull/59) | 类型/模板管理 API、有界分页、字符串 ID/revision、权限与敏感读审计，详见 [API 契约](../development/template-catalog-api.md) |

A3 验证：后端全量测试/Vet、API 边界及 MySQL HTTP 联调 Race、完整 MySQL 数据库回归、Swagger/格式/工作流/Compose、文档链接与 Docker API 构建通过。无 schema/前端变动，不声称 UI 或真实 Java CI 完成；部署版本与 PR 见总看板。

A2b 验证：后端全量测试/Vet、MySQL 8.4 完整数据库回归、存储 Race 连续三轮、Swagger/格式/工作流/Compose 与 Docker API 构建通过。覆盖陈旧 revision、并发唯一发布、停用锁顺序、版本不可改权限、回滚与数据库排版后的限额；写入后 epoch 9 兼容。无前端变更，未声称新增页面或真实 Java CI 验收。预览版本与 PR 见[总进度看板](open-source-production-roadmap.md)。

- [ ] 定义应用类型稳定身份与启停策略，种子含 Java/Python，但支持自定义类型。
- [ ] 建立集中 CI/CD 模板草稿、发布版本及绑定升级策略；发布版本不可变。
- [ ] 定义产物类型、命名槽位、前序引用、参数可覆盖范围、批量/大小上限与 schema_version。
- [ ] 在实现 PR 中补充精确数据库表/索引/约束、API 请求响应、权限到角色的映射和错误码。
- [ ] 针对所有新增对象补迁移 manifest、数据契约、checksum/fingerprint、最小权限账号矩阵；不能修改已发布 epoch 1～8。

### 2.2 B：独立运行与兼容框架

| 增量 | 状态 | 边界 |
| --- | --- | --- |
| B1 | 已合并：[PR #60](https://github.com/go-ree/ares/pull/60) | 固定版本绑定意图预检、类型/环境/归属与参数解析；只读、不保存、不执行，见[契约](../development/binding-preflight.md) |
| B2 | 本次交付，待验收 | CI/CD 专用绑定存储、revision CAS、启停/改绑、受控参数 DTO、管理 API 与 epoch 10 迁移/最小权限验收；不复用旧工作流绑定表，见[契约](../development/binding-storage.md) |
| B3 | 未开始，下一增量 | 独立运行上下文、永久幂等回执、资源关系/快照与旧引擎隔离；C 未就绪不接纳不可执行任务 |

B1 只冻结参数默认值 < 绑定值 < 明确允许的运行覆盖值；HTTP 不接运行覆盖/产物，也不承诺预检后的状态不变。B2 的写入事务已按此重新校验类型、模板、目标与环境状态，不信任预检响应；B3 创建运行时同样必须重新校验，不能信任绑定读取结果。

- [ ] CI 创建使用 app_id，不要求配置环境；CD 使用 config_id、输入 artifact_id 集合、固定模板版本与 revision。
- [ ] 复用 Worker/attempt，而非复制两个调度器；用明确运行种类和上下文识别新旧路径。
- [ ] 新创建操作采用独立语义命名空间的永久回执，覆盖原子快照、稳定顺序锁、重复请求及陈旧 revision。
- [ ] 在 C 完成前，新产物发布入口保持不可用；缺能力返回明确业务错误，不接受不可执行任务、不伪造产物。
- [ ] 保留旧 API/receipt 和 v1/v2 调度语义，新增 API 不调用旧全流程 fallback。

### 2.3 C：产物状态与步骤交接

- [ ] 候选登记与步骤结果原子提交，唯一身份为运行/步骤/attempt/输出名；重放同内容不重复登记，异内容冲突。
- [ ] 生产运行成功门禁与产物发布原子提交；质量检查失败/警告/不明确不发布。
- [ ] 外部存储读取采用受管引用与内容验证，服务层拒绝客户端任意地址。
- [ ] 构建来源 commit、父产物、加工参数版本及最终实际部署引用可追溯；公开响应按白名单输出。
- [ ] 提供明确模拟标识的文件/镜像产物与加工/部署执行器；模拟产物不得进入真实部署。

### 2.4 D/E：交互、迁移和预览

D 的实现栈由 [ADR-0008](../architecture/decisions/0008-frontend-react-semi-migration.md) 决定：**前端改用 React + `@douyinfe/semi-ui`**（Semi 无官方 Vue 版，换组件库等价于换框架）。D 的**范围不变**，但新页面直接在 React + Semi 上开发，不先落在 Vue + Element Plus 上再迁移；其 `services/types/models` 是纯 TS，可先写。迁移本身按 ADR 的 B0～B5 批次分批提交，批次门禁与"迁完即删"规则见该 ADR。

- [ ] 管理者在统一入口配置类型下多种 CI 组合；应用只选版本与参数。
- [ ] CI 运行、产物、CD 运行分别有列表与详情；CD 显示原始/派生/实际部署产物。
- [ ] 多环境可选不同 CD 模板或参数，不复制 CI、不把 CI 步骤写回 AppConfig。
- [ ] 旧应用默认维持旧模式，提供人工确认的类型映射与绑定操作，记录转换审计。
- [ ] 补齐空库模拟 Demo 与旧库 opt-in 演练，完成下面的全栈验收矩阵。
- [ ] 每次交付继续更新固定 `ares-preview`，保留数据与管理员；只有文档变化时不重建相同业务代码。涉及迁移先停旧 Worker、备份再升级，记录部署 commit 和结果，不直接合并 PR。

### 2.5 F：真实能力门禁

在 F 开发前补充具体适配器选择 ADR，比较现有 Jenkins Adapter 扩展与独立执行平台的成本；产品模型与元数据不得依赖最终平台。选择不是让核心再次强制 Jenkins。

- [ ] 至少验证一种真实 Java CI 组合；Python 模板必须绑定真实能力才能启用，不以占位步骤冒充实现。
- [ ] 真实镜像存储按 digest 解析，Agent 加工产生新摘要并保留原镜像。
- [ ] 在隔离目标环境完成部署与历史最终镜像再部署，验证无需重跑 CI。
- [ ] SecretResolver、权限/审计、SSRF、日志脱敏、执行隔离、外部幂等/取消确认全部过关。
- [ ] W11-A～E 仅代表模型与模拟闭环完成；F 未完成不得宣称真实 Java/Python CI 或生产 CD 可用。

## 3. 数据迁移与兼容策略

### 3.1 Expand：旧数据不猜测

新增结构从后续可用 epoch 追加，具体编号由对应代码 PR 根据当时 main 分配，本次 A2a 分配 epoch 9。每个迁移可重复、可中断恢复，有完整 manifest、数据契约与 runtime 授权；新版本按实际兼容区间启动。

- `dev_language` 与 `dev_language_rules` 只导出映射清单。明确匹配才可人工确认关联类型；空值、自定义值、大小写碰撞不静默合并，不因缺类型阻止旧应用历史查询。
- 旧 `release_workflows` / versions / `app_config_workflows` 保留为 legacy combined。即使已有 build/deploy category 也不自动拆分、不发布为可跨应用复用的新模板。
- 旧 task/step/attempt、Output、external_ref、幂等 receipt 保持原样。历史镜像字符串不补造成可信 Artifact；首次新 CD 应选择新 CI 产物，不以旧 tag 猜测摘要。
- 在途旧任务继续按其原始快照由明确的旧路径推进；只由兼容的新二进制承接，不允许旧二进制与不兼容新库混写。禁止 shadow 双执行。

### 3.2 Switch：显式 opt-in

管理员检查应用类型、选定 CI 版本、逐个环境选定 CD 版本及参数；预检全部通过后用 CAS 切换应用模式，不自动触发构建或部署。旧模式未转换时保留旧界面与 API。切换后旧创建入口对新请求返回明确冲突，不静默恢复全流程；既有幂等 key 仍按旧 receipt 重放原结果。

转换时若应用仍有活动 legacy 任务，首版阻止切换并列出待排空任务，避免两种发布同时修改同一环境。此门禁是 Ares 内部边界，不宣称排除了人为外部部署。新 CI/CD 历史存在后不支持“一键切回旧模式”；错误配置通过改绑新版本前向修复。

### 3.3 Contract 与恢复

本工作包不删除旧表、旧列或旧 API，不重写历史 migration。移除需要独立使用量审计与弃用计划。每次升级停止旧 API/Worker、备份并验证恢复，升级 schema、刷新 runtime 表级权限，再验证新版本。失败保留故障库；已越过不兼容 epoch 时必须恢复备份与匹配镜像，不能只回退 Git 分支或修改 ledger。

数据库恢复不撤销已经上传的镜像或已提交的外部部署；回退前必须核对外部对象和执行记录，未知结果不自动重试。

## 4. 验收矩阵

| 场景 | 必须证明的结果 |
| --- | --- |
| Java 多组合 / 自定义类型 | 同类型下可选择至少两套 CI 模板；新增类型无需改枚举 |
| CI 不依赖环境 | 没有 AppConfig 的应用可构建并查看产物，不要求集群 |
| 一次构建、多环境 | 同一 artifact_id 可分别创建两个环境 CD，CI 运行数不增加 |
| CD 加 Agent | 派生新产物，父产物摘要未变，最终部署记录指向派生物 |
| 历史再部署 | 固定最终产物配直接部署模板，不重新构建或重做加工 |
| 类型/归属校验 | wheel 不能误入 JAR 槽位；跨应用和模拟→真实输入被服务端拒绝 |
| 模板与来源冻结 | 发布新模板、改绑环境、移动 branch/tag 均不改变已有运行的已解析 commit/digest |
| 质量门禁失败 | 产物生产步骤成功但后续测试失败时，其他运行不能选用该候选产物 |
| 并发与崩溃 | 多 Worker、重复请求、上传后断连、登记后重启不重复任务/产物，不接受陈旧 fencing |
| 安全重试 | 新 attempt 有新执行键；历史产物来源不覆盖；未知结果与超时不触发新外部副作用 |
| 权限和泄漏 | viewer 无法改模板/发起运行；私有引用/Secret 不进入公开 JSON、日志或审计 |
| 旧库兼容 | 旧任务/日志/receipt 可读和重放；转换不猜历史产物、不双执行；旧在途任务阻止切换 |
| 迁移与重启 | 空库、epoch 8 升级、逐 DDL 中断/并发恢复、最小权限账号、稳定持久化数据均通过 |
| 预览与真实能力 | E 的模拟闭环明确标识；F 使用真实 registry/隔离部署目标提供独立验收证据 |

前端每阶段包含组件测试、类型检查、Lint 与构建；后端包含单测、Race、API/RBAC、真实 MySQL 集成。涉及界面联动的 D/E/F 增加浏览器端到端验证；运行时镜像和供应链继续沿用仓库门禁。没有执行的测试必须标明未执行。

## 5. 同步规则与下一步

### A2a 验证与预览记录

- `4f8a3f8`：epoch 9 / 三张新表 / Java、Python 种子。未实现事务存储或 CAS，不提供管理入口，旧应用、工作流和运行路径不变。
- 后端全量测试、Vet、db/模板 Race、完整 MySQL 8.4 迁移恢复矩阵、账号动态权限测试、Swagger/格式/工作流/Compose 检查与 API 镜像构建通过。前端无改动，未重跑前端测试。
- 停旧 Worker 后备份；导入隔离空库，用 epoch 8 镜像验证 schema 兼容及 3/1/4 关键行数，再升级持久预览。升级后 epoch 9 健康，原数据与密钥保留，类型 2 条、模板/版本 0 条。
- 后续进入 A2b 事务存储/CAS；A3 管理接口和 D 页面仍未开始。PR 链接与评审状态见总看板。

### A1 验证与预览记录

- 后端全量单测、Vet、pipelinetemplate/api/controller Race 通过；校验器约 164 万次模糊测试无异常；Swagger 重生成、格式、工作流与 Compose 配置检查通过。
- 预览后端已更新至 `6dbebd6`，无前端业务变动。停旧服务后备份数据库，保留原数据/密钥卷；服务 healthy，epoch 8 兼容，校验接口匿名返回 401；应用/用户/任务数量保持 3/1/4。
- 本次没有新的数据库迁移或真实 CI/CD 执行，未声称对尚不存在的管理页面进行端到端验收。最终云端结果以 [PR #42](https://github.com/go-ree/ares/pull/42) 检查为准；不直接合并。

本文件记录 W11 明细，[总进度看板](open-source-production-roadmap.md)记录跨工作包顺序；每次实现 PR 同时更新两处状态、PR 链接、实际范围、验证结果、迁移和预览版本。W11-0 已完成；A1/A2/A3 与 B1 已合并，本次交付 B2，下一增量为 B3 独立运行上下文。

### 前端迁移 B1 记录（外壳）

- 交付：React 版 `MainLayout`（Semi `Layout` + `Nav` 权限菜单、用户下拉、改密弹窗）、完整 `Login`（认证方式加载、OIDC 入口、本地登录、首次管理员 bootstrap 字段级校验与错误映射）与正式 `Home`（欢迎/统计/按权限的快捷入口）。B0 的占位外壳删除。
- 权限菜单与 Vue 模板逐项对拍：`visibleMenuItems` 与 `toNavItems` 是纯函数，测试覆盖只读身份不泄漏写入口、父项无可见子项时隐藏、无条件分类保留、用户管理仅 `users:read` 可见；改密入口仅对本地 bootstrap 身份出现（测试实际打开下拉断言）。
- 过渡期取舍见 [ADR-0008 §2.4](../architecture/decisions/0008-frontend-react-semi-migration.md)：未迁移路由禁用而非死链；空父菜单隐藏（对现有角色不可观测）。
- 验证：Vue 栈 21 文件 / 198 测试不退化、React 栈 4 文件 / 31 测试通过（新增 Login 6 项与菜单 8 项）、`tsc` 与 `vue-tsc` 均无错误、ESLint 双栈 0 问题、两条栈构建通过。测试过程中实测并修复了"提交按钮同时挂 onClick 与 htmlType 导致重复提交"的真实缺陷。
- 产物对比：React semi chunk 1004 kB JS + 720 kB CSS，Vue element-plus chunk 1021 kB JS + 361 kB CSS；量级相当，CSS 约为两倍，按组件引入列为可选优化。
- 生产入口未切换，Vue 栈继续对外服务，预览不重启。

### B2 验证与预览记录

- 追加 epoch 10：新增 `application_ci_bindings`、`app_config_cd_bindings` 两张绑定表，唯一键为应用/环境配置，外键 RESTRICT 到目标与不可变版本；不修改已发布迁移、冻结 v1 校验器与 epoch 9 结构。绑定表不冗余类型或种类列，跨种类改绑由存储写入校验与数据契约 `ci-cd-bindings-v1` 双重保证。
- 新增六个管理接口：CI/CD 各自的创建/改绑（revision CAS，省略 `expected_revision` 即创建）与元数据读取，以及受敏感读取审计保护的绑定层参数读取。viewer/releaser 不能改绑定也不能读参数；绑定成功仍返回 `executable=false`，不创建任务、不登记产物。
- 写入事务按 `application_types → pipeline_templates → 绑定行` 固定顺序加锁，重新校验种类、归属、模板/类型/环境启停与固定版本摘要；参数先校验后取锁，存储后回读确认 MySQL 规范化大小。
- 验证：后端全量测试/Vet、相关 Race、完整 `make db-integration`、Swagger 生成、格式/工作流/Compose 与 Docker API 构建通过。新增覆盖逐 DDL 中间态恢复、数据契约 fail-closed、并发创建与并发 CAS、跨种类/跨类型拒绝、软删除与停用分类、最小权限主体拒绝 `DELETE` 与版本改写、HTTP 层组合 grant 联调。
- 账号最小权限动态矩阵同步新增两张表的 `INSERT,UPDATE`：`deploy/compose/mysql/account-init-integration.sh` 的建表清单、DML 白名单、精确权限总数（24 表/44 项 → 26 表/48 项，仍无 `DELETE`）和逐表期望一并更新，`make db-account-integration` 本地通过。该矩阵在云端因未同步而失败过一次，属于本轮新增授权的连带契约。
- 预览升级：部署代码 `05f06ce`（其后提交仅改文档，不重建业务镜像）。先保留旧镜像 `ares-api:preview-epoch9` 并停止业务服务，备份为 `before-w11b2-05f06ce-epoch9.sql.gz`（gzip 校验通过，权限 600）。备份导入隔离 MySQL 8.4 实例后关键行数一致，**epoch 9 镜像报告兼容**，而 epoch 10 镜像报告 `状态=不兼容` 且 `serve` 拒绝启动，证明升级前 fail-closed。随后完整重建（含四个一次性任务）升级持久预览至 epoch 10。
- 升级后：`migrate status` 为 epoch 10 兼容、dirty=0、29 张基础表；3 应用/2 用户/4 任务/2 类型/0 模板/0 版本与升级前一致，两张绑定表为空；`ares_runtime` 对新表只有 INSERT/UPDATE（无 DELETE）。匿名访问四个新接口与 PUT 均为 401，不存在的路由为 404，首页 200；服务健康，账号与持久卷保留。
- 本轮**未执行**已登录的预览正向冒烟：预览管理员凭据不在本会话可用范围内，按约定不重置密码。正向创建/改绑/缺失绑定 404 与最小权限边界由隔离 MySQL 的 HTTP 联调测试覆盖，不把匿名冒烟当成端到端验收。
- [中文 PR #61](https://github.com/go-ree/ares/pull/61) 待评审、未自动合并，8 项云端检查全部成功。本轮没有新页面、没有运行创建，也没有真实 Java/Python CI 能力。

下一次实施**前端框架迁移 B0 骨架**（React 入口、Semi、Zustand 认证、loader 守卫、双 vitest project 与 `frontend-check-react` 门禁），按 [ADR-0008](../architecture/decisions/0008-frontend-react-semi-migration.md) 的批次推进；B3 独立运行上下文与幂等回执、C 产物能力在其后恢复。不先写 CD 按钮、不把旧 Output 临时公开为产物。任何扩展到任意 Shell、自动部署触发、跨应用共享或实际存储平台选择的决定都需补充文档评审。
